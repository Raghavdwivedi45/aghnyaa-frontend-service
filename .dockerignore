# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files. .env must reach the build -- `next build` inlines NEXT_PUBLIC_* into the
# browser bundle, so excluding it would ship empty URLs. Only local overrides are excluded.
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
